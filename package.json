{
    "name": "Food-Order-App",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
    },
    "scripts": {
        "start": "cd backend && nodemon server.js",
        "prebuild": "cd frontend && npm install --force && npm audit fix --force && cd .. && cd backend && npm install",
        "build": "cd frontend && npm install --force && npm audit fix --force  && cd .. && cd backend && npm install"
    }
}